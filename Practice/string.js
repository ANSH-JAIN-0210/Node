const string_handler = {
    lower_case : function(str){
        return str.toLowerCase();
    },
    upper_case : function(str){
        return str.toUpperCase();
    }
}
module.exports = string_handler